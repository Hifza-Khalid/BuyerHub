specVersion: "v1"
serviceId: "madproject"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "madproject-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
